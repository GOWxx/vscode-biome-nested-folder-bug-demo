# biome nested folder config bug demo

## Run project

```bash
  pnpm install
```

## How is the project initialized

```bash
  pnpm init
  pnpm add --save-dev --save-exact @biomejs/biome
  pnpm biome init --jsonc
```